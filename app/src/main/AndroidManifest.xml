<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movietimes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movietimes">
        <activity android:name=".Summary"></activity>
        <activity android:name=".showmovies" />
        <activity
            android:name=".movies"
            android:label="@string/title_activity_movies"
            android:theme="@style/Theme.Movietimes.NoActionBar" />
        <activity
            android:name=".DisplayMovies"
            android:label="@string/title_activity_display_movies"
            android:theme="@style/Theme.Movietimes.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>